-- this is a SQL script that list all rows of the table first_table from the database hbtn_0c_0 in MySQL server
-- the database name will be passed as an argument in the mysql command

SELECT * FROM first_table;